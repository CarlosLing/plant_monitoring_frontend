import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <div className="text-center">
        <h1 style={{ marginTop: "20px" }}>App with React + Django</h1>
      </div>
    );
  }
}

export default Header;